procs:
  web:
    shell: "pnpm run dev:web"
  server:
    shell: "pnpm run dev:server"
  livestream:
    shell: "pnpm run start:livestream-node"
  prisma:
    shell: "pnpm --filter @pakamew/server exec prisma studio"
